Repository content is documentation/configuration oriented. Conventions are defined in CLAUDE.md: use skills/subagents according to routing rules, keep outputs concise, include Mermaid flow diagrams when user flow changes, do not implement directly when delegation is expected, and align new skills/agents with existing markdown frontmatter patterns (name/description/model/color for agents; YAML frontmatter with name/description for skills). Branching and release rules are strict; avoid work on main or release branches unless instructed.