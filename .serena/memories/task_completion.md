When work is completed, verify the exact changed files and run proportional validation. For this repo there are no explicit automated lint/test commands documented in README/CLAUDE, so completion primarily means checking modified markdown/skill definitions for consistency with existing structure and routing rules, then summarizing impacts. Follow higher-level rules from CLAUDE: include Mermaid diagrams when changes affect user flow, create PRs when work is done, and use safe git/release workflows.